# 分析程序提交完全指南

> 本文档面向初学者，详细介绍临床试验分析程序（Analysis Programs）的概念、提交要求和最佳实践。

---

## 目录

1. [什么是分析程序](#1-什么是分析程序)
2. [为什么要提交分析程序](#2-为什么要提交分析程序)
3. [分析程序的类型与范围](#3-分析程序的类型与范围)
4. [程序文件的组织结构](#4-程序文件的组织结构)
5. [程序编写规范](#5-程序编写规范)
6. [常见问题与最佳实践](#6-常见问题与最佳实践)
7. [合规要点](#7-合规要点)

---

## 1. 什么是分析程序

### 1.1 基本概念

**分析程序（Analysis Programs）** 是用于生成临床试验数据和统计结果的计算机代码，通常使用 SAS 或 R 语言编写。

> **小白类比**：
> - 如果 **TLFs** 是考试的"答卷"
> - 那么 **分析程序** 就是"解题过程"——展示答案是怎么算出来的

### 1.2 分析程序的作用

```
分析程序在数据流中的位置
│
│  原始数据（Raw Data）
│      │
│      ▼
│  SDTM 转换程序 ──────→ SDTM 数据
│      │
│      ▼
│  ADaM 派生程序 ──────→ ADaM 数据
│      │
│      ▼
│  TLF 生成程序 ──────→ TLFs（表格/图形/列表）
│      │
│      ▼
│  结果纳入 CSR
│
```

### 1.3 提交程序的目的

| 目的 | 说明 |
|-----|------|
| **可复现性** | 审评员可以重新运行程序验证结果 |
| **透明度** | 展示数据处理和分析的具体逻辑 |
| **可审计性** | 支持 FDA 的数据完整性审查 |
| **问题定位** | 便于解答审评问题 |

---

## 2. 为什么要提交分析程序

### 2.1 FDA 的立场

```
FDA 对分析程序的态度
│
├── 技术上：可选（Not Required）
│   └── 不是网关验证的必需项
│
├── 实际上：强烈建议（Strongly Recommended）
│   ├── 关键性研究（Pivotal Studies）几乎必需
│   ├── 加速审评更加重视
│   └── 缺少程序可能导致审评延误
│
└── FDA 原话：
    "虽然程序不是强制要求，但提交程序
     可以显著加快审评进度，减少信息请求"
```

### 2.2 实际场景

```
场景 1：没有提交程序
┌─────────────────────────────────────────┐
│ 审评员：这个 p 值 0.048 怎么算出来的？  │
│                                         │
│ → 发送信息请求（IR）                    │
│ → 申办方准备程序和说明                  │
│ → 来回沟通 2-4 周                       │
│ → 审评延误                              │
└─────────────────────────────────────────┘

场景 2：提交了程序
┌─────────────────────────────────────────┐
│ 审评员：查看 t_primary_efficacy.sas     │
│                                         │
│ → 看到完整的 PROC MIXED 代码            │
│ → 理解模型设定和参数                    │
│ → 可自行运行验证                        │
│ → 无需额外沟通                          │
└─────────────────────────────────────────┘
```

### 2.3 提交程序的收益

```
提交分析程序的好处
│
├── 对审评的好处
│   ├── 加快审评速度
│   ├── 减少信息请求
│   └── 增强审评信心
│
├── 对申办方的好处
│   ├── 展示数据管理能力
│   ├── 减少后期沟通
│   └── 支持可能的现场核查
│
└── 对行业的好处
    ├── 提高透明度
    └── 建立最佳实践
```

---

## 3. 分析程序的类型与范围

### 3.1 程序类型

```
分析程序分类
│
├── 📁 SDTM 转换程序
│   ├── 作用：原始数据 → SDTM
│   ├── 示例：dm.sas, ae.sas, vs.sas
│   └── 优先级：中等（建议提交）
│
├── 📁 ADaM 派生程序
│   ├── 作用：SDTM → ADaM
│   ├── 示例：adsl.sas, advs.sas, adae.sas
│   └── 优先级：高（强烈建议提交）
│
└── 📁 TLF 生成程序
    ├── 作用：ADaM → TLFs
    ├── 示例：t_demog.sas, f_km_plot.sas
    └── 优先级：高（强烈建议提交）
```

### 3.2 提交范围建议

| 研究类型 | SDTM 程序 | ADaM 程序 | TLF 程序 |
|---------|:--------:|:--------:|:-------:|
| **关键性研究** | 建议 | **强烈建议** | **强烈建议** |
| **支持性研究** | 可选 | 建议 | 建议 |
| **探索性研究** | 可选 | 可选 | 可选 |

### 3.3 程序与输出的对应关系

```
程序命名与输出对应示例
│
├── ADaM 程序
│   ├── adsl.sas → adsl.xpt
│   ├── advs.sas → advs.xpt
│   └── adae.sas → adae.xpt
│
└── TLF 程序
    ├── t_14_1_1.sas → Table 14.1.1 (人口学)
    ├── t_14_2_1.sas → Table 14.2.1 (主要疗效)
    ├── f_14_2_1.sas → Figure 14.2.1 (K-M 曲线)
    └── l_16_2_1.sas → Listing 16.2.1 (受试者列表)
```

---

## 4. 程序文件的组织结构

### 4.1 目录结构

```
eCTD 中程序的位置
m5/
├── datasets/
│   ├── sdtm/
│   └── adam/
│
└── programs/                    ← 程序目录
    │
    ├── sdtm/                    ← SDTM 转换程序
    │   ├── dm.sas
    │   ├── ae.sas
    │   ├── vs.sas
    │   └── ...
    │
    ├── adam/                    ← ADaM 派生程序
    │   ├── adsl.sas
    │   ├── advs.sas
    │   ├── adae.sas
    │   └── ...
    │
    └── tlf/                     ← TLF 生成程序
        ├── tables/
        │   ├── t_14_1_1.sas
        │   └── ...
        ├── figures/
        │   ├── f_14_2_1.sas
        │   └── ...
        └── listings/
            ├── l_16_2_1.sas
            └── ...
```

### 4.2 辅助文件

```
程序目录可能包含的辅助文件
│
├── 📄 readme.txt / readme.pdf
│   └── 程序使用说明，运行顺序
│
├── 📄 macros/
│   └── 公共宏程序
│
├── 📄 formats/
│   └── 自定义格式（format catalog）
│
└── 📄 setup/
    └── 环境设置程序（libname, options）
```

### 4.3 README 文件内容

```
README 文件建议包含
│
├── 1. 程序概述
│   └── 程序的总体结构和目的
│
├── 2. 运行环境
│   ├── SAS 版本（如 SAS 9.4）
│   ├── 操作系统（如 Windows Server）
│   └── 所需的 SAS 产品（BASE, STAT, GRAPH）
│
├── 3. 目录结构说明
│   └── 各子目录的内容
│
├── 4. 运行顺序
│   └── 程序的执行顺序（如先运行 SDTM，再 ADaM）
│
├── 5. 依赖关系
│   └── 宏程序、格式等依赖项
│
└── 6. 已知问题
    └── 任何已知的限制或注意事项
```

---

## 5. 程序编写规范

### 5.1 程序头部信息

```sas
/******************************************************************************
* 程序名称：  t_14_2_1.sas
* 程序描述：  Table 14.2.1 - 主要疗效终点分析（收缩压变化）
* 输入数据：  adam.advs, adam.adsl
* 输出文件：  t_14_2_1.rtf
*
* 创建者：    张三
* 创建日期：  2024-01-15
*
* 修改历史：
* 日期        修改人    描述
* ----------  --------  ----------------------------------------------------
* 2024-01-20  李四      根据 SAP 修订版更新协变量
* 2024-02-01  张三      修复分层变量的缺失值处理
*
* 参考文档：  SAP v2.0 Section 8.1
******************************************************************************/
```

### 5.2 代码规范

```sas
/*--- 良好的代码规范示例 ---*/

/* 1. 清晰的注释 */
/* 筛选主要疗效分析人群：FAS，第12周访视，收缩压参数 */
data work.eff_pop;
    set adam.advs;
    where fasfl = 'Y'
      and avisitn = 12
      and paramcd = 'SYSBP'
      and anl01fl = 'Y';
run;

/* 2. 有意义的变量名 */
proc means data=work.eff_pop noprint;
    class trt01pn;
    var chg;
    output out=work.stats
           n=n_subjects
           mean=mean_chg
           std=std_chg;
run;

/* 3. 主要分析：ANCOVA */
/* 模型：CHG = TRT01PN + BASE + SITEID */
/* 参考：SAP Section 8.1.2 */
proc mixed data=work.eff_pop;
    class trt01pn siteid;
    model chg = trt01pn base / solution;
    random siteid;
    lsmeans trt01pn / diff cl alpha=0.05;
    ods output lsmeans=work.lsmeans
               diffs=work.diffs;
run;
```

### 5.3 避免的做法

```sas
/*--- 应避免的编程实践 ---*/

/* ❌ 硬编码路径 */
libname adam "C:\Users\zhangsan\project\data";  /* 不可移植 */

/* ✅ 使用相对路径或参数化 */
%let root = .;
libname adam "&root./data/adam";

/* ❌ 没有注释的复杂逻辑 */
if a=1 and (b in (2,3) or c>5) and d^=. then flag=1;

/* ✅ 添加解释性注释 */
/* 标记符合以下条件的受试者：
   - 在全分析集 (a=1)
   - 完成关键访视 (b in 2,3) 或达到终点 (c>5)
   - 有有效的主要终点数据 (d非缺失) */
if a=1 and (b in (2,3) or c>5) and d^=. then flag=1;

/* ❌ 使用 PROC SQL 删除表但不检查 */
proc sql; drop table work.temp; quit;  /* 如果表不存在会报错 */

/* ✅ 安全的清理方式 */
proc datasets lib=work nolist;
    delete temp;
quit;
```

### 5.4 可移植性要求

```
程序可移植性检查清单
│
├── ✅ 不使用绝对路径
│   └── 使用相对路径或宏变量定义根目录
│
├── ✅ 不依赖本地环境
│   └── 不引用本地安装的非标准宏或格式
│
├── ✅ 包含所有依赖项
│   └── 自定义宏、格式随程序一起提交
│
├── ✅ 明确 SAS 版本
│   └── 在 README 中说明测试环境
│
└── ✅ 输出文件名一致
    └── 输出文件名与 TLF 编号对应
```

---

## 6. 常见问题与最佳实践

### 6.1 常见问题

#### Q1: 必须使用 SAS 吗？

```
支持的编程语言
│
├── SAS
│   ├── 状态：最广泛使用，FDA 审评员熟悉
│   ├── 优点：行业标准，XPT 文件原生支持
│   └── 推荐：优先使用
│
├── R
│   ├── 状态：FDA 已接受，使用增加中
│   ├── 优点：免费开源，统计功能强大
│   ├── 注意：需要提供 sessionInfo()
│   └── 推荐：可以使用，但需充分文档化
│
└── Python
    ├── 状态：接受度正在提高
    ├── 注意：需要额外的验证说明
    └── 推荐：非主流选择，需谨慎
```

#### Q2: 程序需要能在 FDA 端运行吗？

```
FDA 对程序可运行性的期望
│
├── 理想情况：程序可以直接运行
│   └── FDA 可以重现结果
│
├── 现实情况：
│   ├── 数据路径需要调整
│   ├── 某些宏/格式可能缺失
│   └── 环境差异可能存在
│
└── 最低要求：
    ├── 程序逻辑清晰可读
    ├── 关键分析步骤有注释
    └── 可以理解分析过程（即使不能直接运行）
```

#### Q3: 宏程序要展开还是保持调用形式？

```
宏程序的处理方式
│
├── 选项 1：保持宏调用 + 提交宏源码
│   ├── 优点：代码简洁，反映实际开发方式
│   └── 要求：必须同时提交宏程序
│
├── 选项 2：展开宏为原始代码
│   ├── 优点：审评员可直接看到完整逻辑
│   └── 缺点：代码冗长，可能引入错误
│
└── 推荐：选项 1（保持宏调用 + 提交宏源码）
    └── 在 macros/ 子目录提交所有自定义宏
```

### 6.2 最佳实践

```
✅ 分析程序最佳实践清单
│
├── 组织结构
│   ├── 按类型分目录（sdtm/adam/tlf）
│   ├── 提供 README 说明文件
│   └── 包含所有依赖的宏和格式
│
├── 代码质量
│   ├── 完整的程序头部信息
│   ├── 充分的代码注释
│   ├── 与 SAP 章节对应
│   └── 有意义的变量和数据集命名
│
├── 可移植性
│   ├── 使用相对路径
│   ├── 不依赖本地环境
│   └── 明确软件版本要求
│
└── 质量控制
    ├── 程序经过独立验证（QC）
    ├── 输出与提交的 TLF 一致
    └── 关键程序有双人编程
```

---

## 7. 合规要点

### 7.1 FDA 技术拒收标准（TRC）相关

```
分析程序与 TRC 的关系
│
├── 直接要求：无
│   └── TRC 不将程序列为必需项
│
├── 间接影响：
│   ├── 缺少程序可能导致信息请求
│   ├── 关键分析无法验证会延误审评
│   └── 可能影响审评结论的信心
│
└── FDA 期望：
    └── "关键性研究强烈建议提交分析程序"
```

### 7.2 提交前检查清单

```
程序提交前检查清单
│
├── 📁 文件组织
│   ├── [ ] 目录结构清晰
│   ├── [ ] README 文件完整
│   └── [ ] 程序命名与输出对应
│
├── 📝 代码质量
│   ├── [ ] 程序头部信息完整
│   ├── [ ] 注释充分且准确
│   ├── [ ] 与 SAP 一致
│   └── [ ] 变量名有意义
│
├── 🔧 可移植性
│   ├── [ ] 无绝对路径
│   ├── [ ] 依赖项随附提交
│   ├── [ ] 环境要求文档化
│   └── [ ] 在干净环境测试过
│
└── ✅ 验证
    ├── [ ] 程序输出与提交 TLF 一致
    ├── [ ] QC 程序/日志可用
    └── [ ] 无错误和关键警告
```

### 7.3 程序与其他提交物的关联

```
程序与其他提交物的对应关系
│
├── 程序 ↔ 数据
│   ├── SDTM 程序输出 → sdtm/*.xpt
│   └── ADaM 程序输出 → adam/*.xpt
│
├── 程序 ↔ TLFs
│   └── TLF 程序输出 → reports/tables, figures, listings
│
├── 程序 ↔ Define.xml
│   └── 派生方法与 Define.xml 中的 Origin/Method 一致
│
├── 程序 ↔ ADRG
│   └── 在 ADRG 中引用程序，说明关键实现
│
└── 程序 ↔ ARM（如适用）
    └── ARM 中的 AnalysisProgram 指向正确的程序文件
```

---

## 附录：快速记忆

```
分析程序 = SDTM程序 + ADaM程序 + TLF程序

提交状态：技术可选，实际强烈建议（关键研究）

目录位置：m5/programs/
├── sdtm/（转换程序）
├── adam/（派生程序）
└── tlf/（输出程序）

关键要求：
├── 代码清晰可读
├── 注释充分
├── 可移植（无绝对路径）
├── 包含依赖（宏、格式）
└── 输出与 TLF 一致

语言选择：SAS（首选）、R（可接受）
```

---

## 相关文档

| 想了解... | 阅读文档 |
|----------|---------|
| 提交包整体结构 | [Submission_Package_Guide.md](./Submission_Package_Guide.md) |
| ADaM 数据标准 | [ADaM_Complete_Guide.md](./ADaM_Complete_Guide.md) |
| SAP 统计分析计划 | [SAP_Tutorial.md](./SAP_Tutorial.md) |
| ARM 分析结果元数据 | [ARM_Guide.md](./ARM_Guide.md) |
| 审评员指南 ADRG | [Reviewers_Guide.md](./Reviewers_Guide.md) |
| CSR 与 TLFs | [CSR_TLFs_Guide.md](./CSR_TLFs_Guide.md) |

---

> **参考资源**
> - [FDA Study Data Technical Conformance Guide](https://www.fda.gov/regulatory-information/search-fda-guidance-documents/study-data-technical-conformance-guide)
> - [PhUSE Best Practices for Analysis Programs](https://phuse.global/)
> - [CDISC Analysis Data Model (ADaM)](https://www.cdisc.org/standards/foundational/adam)

---

*本文档供学习参考，如有疑问请咨询专业人士。*
